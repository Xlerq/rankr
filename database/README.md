# Database

This directory is reserved for database-related documentation and future setup notes.

The planned database is SurrealDB. The MVP should store raw or processed GPW/WIG20 end-of-day datasets and ranking outputs once implementation begins.

No schema files, migrations, or database code are included in this initial structure.

